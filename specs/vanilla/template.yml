---
Template:
  using range literal works as expected:
  - TPL: "{% assign nums = (x..y) %}{% for num in nums %}{{ num }}{% endfor %}"
    EXP: '12345'
    CTX: { x: 1, "y": 5 }
  - TPL: "{% assign foo = (x..y) %}{{ foo }}"
    EXP: 1..5
    CTX: { x: 1, "y": 5 }
