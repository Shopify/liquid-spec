- template: "{{ array }}"
  environment:
    array:
    - hello
    - "hel\nlo"
    - nested:
      - foo
      - bar
  expected: "hellohel\nlo{\"nested\"=>[\"foo\", \"bar\"]}"
  name: Array to_s handles escape sequences