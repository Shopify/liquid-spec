---
name: "Liquid Ruby"
description: "Core Liquid template engine behavior from Shopify/liquid"

default: true

hint: |
  These specs test core Liquid functionality from the reference implementation.
  Failures indicate missing or incorrect Liquid behavior.

# No suite-level features requirement, so JSON-RPC adapters can run portable specs.
# Individual specs that need Ruby-specific drops declare features: [ruby_drops].

# Feature documentation:
# - :core includes :ruby_drops (full implementation)
# - JSON-RPC adapters that can't support portable drops should list :drops in missing_features.

defaults:
  render_errors: false
