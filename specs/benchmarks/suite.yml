---
name: "Benchmarks"
description: "Performance benchmarks for Liquid implementations"

default: false

timings: true
default_iteration_seconds: 5

hint: |
  These specs are designed for performance measurement.
  Run with: liquid-spec bench my_adapter.rb

features:

defaults:
  render_errors: false
